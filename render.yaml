services:
  - type: web
    name: smartprice-ai
    env: node
    rootDir: backend
    buildCommand: npm install && npm run build
    startCommand: node lib/index.js
    autoDeploy: true
    healthCheckPath: /
    envVars:
      - key: GEMINI_API_KEY
        value: REPLACE_WITH_YOUR_KEY
